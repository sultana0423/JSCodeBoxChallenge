/*Two variables are given key and value. Return these values in JSON notation, but as a string, not an JSON object.*/
/* Example: {"name": "JSCodebox"}*/

function firstJSON(key, value) {
  return `{"${key}":"${value}"}`;
}
